#include <iostream>
#include<string>
using namespace std;
    int main()
    {
        string s, space, space2, s2;
        int num1 , num2;
        cin >> s >> space >> num1 >> space2 >> num2 >> s2;

        if(s == "Original:")
        {
            cout << space << space2 << endl;
        }

        return 0;
    }